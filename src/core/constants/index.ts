export {
  SessionEvents,
  PromptEvents,
  CognitiveEvents,
  GhostTextEvents,
  ResponseEvents,
  InsightEvents,
  HardwareEvents,
} from '../event-bus/registry';

export type { EventType } from '../event-bus/registry';

